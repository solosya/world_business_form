{{ set(this, 'title', 'Contact Us') }}  

<div class="page_section">
    <section class="container">
        <!-- Begin ContactUs -->
        <div class="content-wrap contact_us_page">
            <div class="row">
                <div class="col-third">
                    <div class="contact_details">
                        <h3 class="sub-heading">Contact us</h3>
                        <p class="has-border-bottom">You can contact us direct by:</p>

                        {% set businessDetails = _Blog.getHomeBlogSettings() %}
                        <ul class="address">
                            <li><span>Email</span> <a class="green" href="mailto:{{businessDetails.contact['email']}}">{{businessDetails.contact['email']}}</a></li>
                            <li><span>Postal</span> {{businessDetails.contact['address']}}<br />
                                {{businessDetails.contact['city']}}, {{businessDetails.contact['state']}}
                                <br />
                                {{businessDetails.contact['country']}} {{businessDetails.contact['postcode']}}</li>
                        </ul>
                        <ul class="social">
                            {% if businessDetails.social['facebook'] is not empty %}
                                <li class="facebook"><a href="{{businessDetails.social['facebook']}}" target="_blank"><i class="fa fa-facebook"></i></a></li>
                            {% endif %}
                            {% if businessDetails.social['twitter'] is not empty %}
                                <li class="twitter"><a href="{{businessDetails.social['twitter']}}" target="_blank"><i class="fa fa-twitter"></i></a></li>
                            {% endif %}
                            {% if businessDetails.social['google'] is not empty %}
                                <li class="g-plus"><a href="{{businessDetails.social['google']}}" target="_blank"><i class="fa fa-google-plus"></i></a></li>
                            {% endif %}
                        </ul>
                    </div>
                </div>

            </div>
        </div>
    </section>
</div>