<div class="row">
     <section class="col-sm-12">
        <div class="row">
            <div id="{{container}}" 
                 class="clearfix"
                 data-offset="{{limit}}"
                 data-blogid="{{blogid}}"
                 data-articlecount="10"
                 data-containerclass="{{card_style}}"
                 data-existing-nonpinned-count="{{non_pinned}}">
             </div>
            <a class="loadMoreArticles" 
                data-container="{{container}}"
                href="#">Load more articles</a>
        </div>

        {% set noDataCls = 'hide' %}
        {% if articles|length == 0 %}
            {% set noDataCls = '' %}
         {% endif %}
        <div class="row text-center has-margin-bottom-20 has-margin-top-20 {{noDataCls}} NoArticlesMsg">
            <p class="no-data text-center">No articles found.</p>  
        </div>
    </section>
</div>
